services:
  devcontainer:
    extends:
      file: docker-compose.yaml
      service: devcontainer
    environment:
      - ASR_CUDA=1
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
