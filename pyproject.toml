[build-system]
requires = ["setuptools >= 64.0"]
build-backend = "setuptools.build_meta"

[project]
name = "youtube-whisperer"
version = "0.0.1"
description = "Download video and run speech recognition"
requires-python = ">=3.10"
authors = [
  {name = "Libo Yin", email = "liboyin830@gmail.com"}
]
dependencies = [
  "azure-cognitiveservices-speech",
  "ctranslate2",
  "fastapi[standard]",
  "faster-whisper",
  "ffmpeg-python",
  "more-itertools",
  "pathlib-extensions@git+https://github.com/liboyin/pathlib-extensions.git",
  "redis",
  "soundfile",
  "youtube-transcript-api",
  "yt-dlp[default]",
]

[project.optional-dependencies]
dev = [
  "ipython",
  "mypy",
  "pytest",
  "pytest-cov",
  "pytest-mock",
  "pytest-randomly",
]

[tool.mypy]
ignore_missing_imports = true
check_untyped_defs = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_unused_configs = true
strict_optional = true

[tool.pytest.ini_options]
addopts = "--cov=youtube_whisperer --cov-report html --cov-report=term"

[tool.setuptools.packages]
find = {where = ["."]}
