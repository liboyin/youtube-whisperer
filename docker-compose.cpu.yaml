services:
  prefect-worker:
    extends:
      file: docker-compose.yaml
      service: prefect-worker
    environment:
      - ASR_CUDA=0
  devcontainer:
    extends:
      file: docker-compose.yaml
      service: devcontainer
    environment:
      - ASR_CUDA=0
